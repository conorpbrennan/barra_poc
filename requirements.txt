# Frame builders (barra_build_frames.py, barra_build_frames_v1.py)
numpy==2.4.6
pandas==2.3.3
requests==2.34.2
pyarrow==24.0.0      # parquet read/write backend for pandas

# Cube only (barra_factor_risk_cube.py)
atoti==0.9.15

# Per-view risk-analyst commentary (risk_api.py /analysis): plain Messages API, no tools.
anthropic==0.111.0

# Excel validation workbook (barra_excel_check.py)
openpyxl==3.1.5

# Direct-Atoti demo notebook (notebooks/13f_risk.ipynb) — altair/matplotlib already pulled
# in by atoti; these add the JupyterLab server + kernel.
jupyterlab==4.5.8
ipykernel==7.3.0
# The notebook's manager dropdown (notebook_helpers.manager_picker). NOT stageable via
# data/_pylibs like altair/narwhals: ipywidgets ships its frontend as a JupyterLab *labextension*
# (jupyterlab_widgets), which JupyterLab discovers from jupyter data dirs, not from PYTHONPATH —
# so the container needs either this in the image or a staged JUPYTER_PATH (docker/README.md).
# The notebook degrades to a plain validated selection when it is absent.
ipywidgets==8.1.9
